export enum Exchange {
  MOEX = 'MOEX',
}

export enum DealType {
  BUY = 'BUY',
  SELL = 'SELL'
}

export enum SecurityType {
  SHARE = 'SHARE',
  BOND = 'BOND',
}

export enum Role {
	ADMIN = 'ADMIN',
	INVESTOR = 'INVESTOR'
}