export enum Exchange {
  MOEX = 'MOEX',
}

export enum DealType {
  BUY = 'BUY',
  SELL = 'SELL'
}

export enum TransactionType {
  CASHOUT = 'CASHOUT',
  DEPOSIT = 'DEPOSIT'
}

export enum SecurityType {
  SHARE = 'SHARE',
  BOND = 'BOND',
  PIF = 'PIF',
  INDEX = 'INDEX',
  FUTURES = 'FUTURES'
}

export enum Role {
	ADMIN = 'ADMIN',
	INVESTOR = 'INVESTOR'
}